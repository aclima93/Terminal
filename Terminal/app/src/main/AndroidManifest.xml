<?xml version="1.0" encoding="utf-8"?>
<manifest package="pt.uc.student.aclima.terminal"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CollectorsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <service
            android:name=".Collectors.PeriodicDataCollector.PeriodicDataCollector"
            android:exported="false">
        </service>

        <receiver
            android:name=".Collectors.CollectorsReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.TIME_SET"/>
                <action android:name="android.intent.action.DATE_CHANGED"/>
                <action android:name="android.intent.action.TIMEZONE_CHANGED"/>
            </intent-filter>
        </receiver>

        <service
            android:name=".Collectors.EventfulDataCollector.EventfulDataCollector"
            android:exported="false">
        </service>
    </application>

</manifest>